# ST185 RealDash — ROUND 4 Progress Ledger — CLOSED 2026-07-08 (ROUND 4 COMPLETE)

> This ledger is CLOSED. All learnings are folded into the skill: `C:\Users\danie\.cursor\skills\realdash-dashboard-builder\`. Round-3 history: `_build\progress_ledger.md` (closed). New work → start a round-5 ledger.

## Environment
- RealDash 1.92 maximized. User left ACTIVE DASH = v5, SIMULATION MODE ON (~19 s cycle; sim never raises FLAT/FAN bits).
- Dashes live in `C:\Users\danie\Downloads\` (load source) and `C:\projects\st185-link-ecu-config\rd-build\realdash-root\` (deploy target).
- User's annotated screenshots for this round: `C:\Users\danie\.cursor\projects\c-projects-realdash-rd-build-plan\assets\` (9 images, image-a916... through image-a544...).
- User asset folders: fonts `C:\projects\Fonts-20260708T013110Z-3-001`; images `C:\Users\danie\.personal OneDrive\OneDrive\Desktop\Celica\Dashboard\PowerTune\Full PowerTune layouts\Corsa\CORSA v1.1_Dash wLights\Logos` and `...\PowerTune\Full PowerTune layouts\ECU Master`.
- KEEP ALL EXISTING LAYOUTS (user likes having choices). Back up every .rd before editing.

## Global conventions (user rules, apply everywhere)
- Turbo notation: label "Turbo", units " k RPM" (value still =V/1000 gauge math).
- Units in dim chips beside values, never in labels naming the measurement. % may stay in labels.
- Trigger Error: remove where it occupies large/spanning tiles; omit entirely from all NEW dashes.
- Hero gauges on new dashes must come from: Turbo "k RPM", Throttle "%", Engine Load "%".
- Never validate the user's assertions if wrong — verify (e.g. EFR 7163 max speed) and record the truth.

## STAGE P — Prep/research (parallel-safe, NO RealDash interaction)
- [x] Inventory fonts folder; identify draco/aerospace/racehead files. (34 files; Draco.otf, Aerospace.otf+.ttf, RaceHead.ttf; no license files)
- [x] Research RealDash custom font support + exact procedure (manual/forum). (YES — import TTF/OTF via Look'n Feel→Font & Text; fonts embed in .rd, no device install; dev-confirmed)
- [x] Verify BorgWarner EFR 7163 max turbo speed from spec; recommend v4 dial max (user suggested 300k — check). (Spec = 150,000 rpm; 300k is 2x too high; recommend 160k dial max, red band 150-160k)
- [x] Download 2-3 glare-free seamless carbon fiber weave textures (angled twill) → `_build\assets\carbon\` (verify with PIL). (3 kept @2048x2048: ambientCG CC0 twill, ShareTextures CC0 45°-angled twill, Texturize basketweave; 1 rejected)
- [x] Inventory both OneDrive image folders; copy useful assets → `_build\assets\imported\`. (14 PNGs copied + manifest.md; best find: 12-frame progressive shift-light strip)
- [x] Extract from `link_g4x_realdash.xml`: all TC-related channels (active/mode/intervention %), CRUISE and A/C enum values. (TC Setting 0-4 + TC Intervention 0-100% on 0x3EF; no discrete active bit — use Intervention>0; CRUISE 0-4 OFF/STBY/SET/RES/OVR; AC 0-3 OFF/REQ/ON/FLT)
- [x] Deliverable: `_build\research\round4_prep.md`.

## STAGE A1 — Fixes: probes, v1, v2, v3 (needs app)
- [x] probe_base.rd: archive to `_build\backup\`, remove from Downloads (user: test dash, delete). (archived as r4_20260707_220511_probe_base.rd)
- [x] probe_gm.rd → **st185_dash_v6.rd** (probe_gm removed from Downloads, backed up). TRIGGER ERR tile removed; TARGET λ tile now spans rows 1-2 (col4), Turbo tile spans rows 2-3 (col3) with h70 value + 'k RPM' chip; ENGINE LOAD % moved to row3 col4; labels → chip convention (CHARGE IAT+°C, COOLANT P+kPa, FUEL TEMP+°C); CRUISE/A-C framed state lenses (v1 recipe). Verified in sim.
- [x] v1 (st185_dash.rd): CRUISE/A-C framed state lenses w/ state words (verified in sim: OFF/STBY/SET/RES/OVR + OFF/REQ/ON/FLT all render); TRIGGER ERR tile removed, FUEL TEMP + ETHANOL % widened to fill row 3.
- [x] v2: sweep rebuilt as 16 uniform 45px segments (pitch 49, gap 4), one record each, dark cell -> lit at k*6.25% (yellow/orange/red zones); old 3-big+overlay scheme gone. Verified in sim at 22% and 92%.
- [x] v3: left column moved up, Turbo value h78 tall + 'k RPM' chip; TRIGGER ERR + SYNC HEALTH removed; center TRACTION CONTROL panel: MODE (TC Setting), INTERVENTION % (0x5A59FBBD), amber 'TC ACTIVE' lens lit at intervention >=1%. Verified in sim (mode 1-3, intervention 9/94, lens lit).
- [x] Turbo "k RPM" notation applied to v1, v2, v3, v6 (label "Turbo", dim "k RPM" chip, =V/1000 math retained; ranges normalized to post-math 0/255 warn180/crit190).
- [x] FAN ANSWER: on v1/v2/v3/v6 the FAN pill is bound to ST185: Radiator Fan (bit, hash 0xC44AB478) with a flag=1 dynamic bg fill (dark→lit), flag=1 text (dim→contrast), blink=0. When the bit goes 1 the pill fills SOLID STEADY (no blink): v1+v6 bright blue FF34A8FF w/ near-white text; v2 green FF3DDC5A w/ near-black text; v3 cyan FF00F0FF w/ dark text. CAVEAT: as shipped from round 3 they could NEVER light (±999 alarm recipe disables the fill — see 23:35 discovery); fixed on all four (warn .5/crit .7/below 0), force-tested + sim-verified lit. v5 likely has the same latent bug → Stage A2.
- [x] Verified all in sim (bursts r4_v1t_*, r4_v2_*, r4_v3_*, r4_v6_*), deployed to Downloads + realdash-root, screenshots `_build\shots\r4_v1.png/r4_v2.png/r4_v3.png/r4_v6.png`. v5 restored as active dash.

## STAGE A2 — Fixes: v4, v5 (needs app; consumes Stage P outputs)
- [x] v4 small arcs: DELETE tiny in-arc numbers → large major tick marks (optionally color-shift with fill/active). Add MORE arc gauges to v4. (numbers hidden via arc maxdig=0; threshold-lit 6px tick dots outside each ring, dim brass → cream/amber lit, red at crit; 3 NEW arcs GUI-added + bound binarily: CHARGE TEMP, FUEL TEMP, ETHANOL %)
- [x] v4 turbo dial: max 160 (k units via needle gauge-math =V/1000), de-zeroed labels 0/40/80/120/160 (seg5 maxdig3), warn140/crit150, baked red band 150-160k on the repainted face, "TURBO k RPM" caption INSIDE dial (text record painted after needles), external readout+label+k chip removed.
- [x] v4 needle/styling experiments: it2 = repainted roundface (matte near-black, slim gunmetal bezel, baked 5 majors + minors + red band) + arr2-tint test (tinted labels, not needle); it3r = needle.png spliced neon RED w/ white-hot core; it3b = neon BLUE variant; chose RED (matches warm brass/amber Podium palette; blue clashed).
- [x] v5 carbon background: replace generated weave with REAL glare-free carbon sample (from `_build\assets\carbon\`), angled weave. (ShareTextures 45° twill CC0, 2048→512 tile, brightness ×0.30 contrast ×1.05; wells/strip/track re-baked over it; cf_sheet2.png spliced over `_indicators.png_`; a2_gen_cf2.py)
- [x] v5 pills: lit overlay corners overlap pill rounded corners (wonky) → REBAKED wells near-square (r=2, inset 8) so the square lens gauge fits exactly w/ 2px reveal; 6 halo records parked (their square glow corners over the rounded bezel were the worst offender), repurposed for CRUISE/A-C state stacks.
- [x] v5 CRUISE/A-C: state text inside the colored area. (r4_v1 level-band recipe: fill1+fill2+3/2 word records; CRUISE OFF dark/STBY steel/SET blue/RES teal/OVR amber, A/C OFF/REQ amber/ON blue/FLT red; sim-verified SET/STBY/RES/ON/REQ/FLT with words)
- [x] Turbo "k RPM" notation on v4, v5. Verify, deploy, screenshots. (v4: in-dial "TURBO k RPM" caption, external label/readout gone; v5: label "Turbo" + "k RPM" chip. Sim bursts a2_v4_ver_*/r4v5fin_*; deployed to Downloads + realdash-root; shots r4_v4.png/r4_v5.png + r4_v4_zoom_dial/arcs + r4_v5_zoom_pills_lit/unlit + r4_v5_zoom_states; v5 left active, sim ON; probe .rd files removed from Downloads)

## STAGE B — Three NEW dashes (v7, v8, v9; needs app; consumes Stage P)
- [ ] Use custom fonts (draco/aerospace/racehead) if RealDash supports them (per Stage P), custom images from user folders, widgets/features not yet used. No Trigger Error. Heroes only from: Turbo "k RPM", Throttle %, Engine Load %.
- [ ] Polished, distinct design languages; verify in sim; deploy; screenshots.

## STAGE B1 — Font workflow + v7 (needs app)
- [x] Font donor: clone an existing .rd to Downloads temp name; baseline in-app save. (font_donor.rd from v1; baseline save 99646B/120rec — save dropped the 2 inert pads TG189/TG62, rest of order intact)
- [x] GUI-import RaceHead.ttf; apply to a text gauge; in-app save; snapshot; verify render. (applied to TG34 THROTTLE value; renders chunky racing digits)
- [x] GUI-import Aerospace (.ttf); save; snapshot; verify render. (TG35 BOOST MAP)
- [x] GUI-import Draco.otf; save; snapshot; verify render. (TG36 TC; italic speed style) — NO importer crashes on any of the 3
- [x] Binary-diff snapshots: font asset storage format + per-gauge font lp-string reference. Documented below (FONT BYTE FORMAT).

### FONT BYTE FORMAT (B1 discovery, for Stage B2/C)
- GUI import path (physical px, maximized): select text gauge → LOOK'N FEEL (688,165) → FONT & TEXT (163,617) → SELECT FONT button (881,111) → SELECT ASSET screen. "+" tile opens a Win32 Open dialog (filter *.ttf/*.otf): File-name field (572,686), clear w/ End+backspaces, type FULL path, Enter. Font becomes a tile (preview rendered in itself); click tile → DONE checkmark (133,110) applies it to the selected gauge. Tile slots: row pitch ~480px: slot centers ≈ (240,300),(720,300),(1198,300),(1678,300), wraps to y≈585. CAUTION: the "+" position shifts right as fonts accumulate (new fonts insert at slot 0).
- GOTCHA (cost 20 min): clicking with PREVIEW-image coords instead of physical px silently closes SELECT ASSET (looks like a broken importer). App restart is NOT needed — it was a coordinate bug, importer is fine.
- .rd storage: font assets live in the HEADER right after the '2.4.1' version lpstr: u32 asset_count, then per font: u32 type_tag=5, lpstr filename, u32 data_size, 24B meta (u32 0, u32 1, u32 0x46, u32 1, f32 1.0, u32 0), then the RAW UNMODIFIED ttf/otf bytes (data_size = exact source file length; verified byte-identical for RaceHead 51776B). Assets back-to-back; original header tail (u32 1, 0x0000c353 f32, canvas bg, count) follows the last one.
- Per-gauge reference: the font lp-string right after $#V2#$ (G2.font) = the asset FILENAME, e.g. 'RaceHead.ttf' (default 'defaultdashfont'). Setting it = re-splice like set_gauge_math (shifts later offsets — do early per gauge).
- Font-size behavior unchanged: em-size still = gauge rect height.
- Clean font donor for harvesting: `_build\assets\font_donor_3fonts.rd` (v1 layout + all 3 fonts embedded + refs on TG34/35/36).
- [ ] Keep a clean font-donor .rd in `_build\assets\` for Stage B2/B3 asset harvesting.
- [x] v7 GUI phase: LED sheet imported as Image gauge asset, subframes 1x12 set, bound to ST185: Throttle 0-100, in-app save. (VERDICT: 1.92 subframes are STATIC atlas sampling only — no value-driven frame selection; value animation needs the 2.4.7+ XML system. Used frame 1 = unlit rail art + 9 binary text-gauge LED lenses on top.)
- [x] v7 binary build (r4_v7.py): "APEX NIGHT" — 12 channels, no Trigger Err, heroes Turbo + Throttle (RaceHead h92), 6 pills w/ FAN steady recipe, CRUISE/A-C state words, k RPM chip, unit chips, 3 custom fonts.
- [x] v7 visual iteration x3 in sim (it1 image-order blackout; it2 fonts live + overflow fixes; it3 LED alignment + chip fonts); 26-frame burst verified LED sweep 0->95% (blue x3 / green x4 / purple / red), state words RES/OVR/ON/FLT, pills lit+unlit steady. Screenshots `_build\shots\r4_v7.png` + `r4_v7_zoom_font_led.png`.
- [x] Deployed to Downloads + realdash-root (276834B); font_donor archived to `_build\backup\r4_20260708_font_donor_final.rd` + clean copy `_build\assets\font_donor_3fonts.rd`; v5 reloaded active, SIM ON.

### V7 build gotchas (B1, for Stage B2/B3/C)
- **Per-level fonts are what renders.** g.font (post-$#V2#$) alone does nothing. Use `G2.set_level_fonts('RaceHead.ttf')` (new in rd_lib2) = 3 lpstrs after the 4 color groups (arr_marker+4+128+12). An in-app save STRIPS font strings the renderer didn't accept, silently reverting them.
- **GUI-added Image gauge KILLS all text records painted after it** on this donor (unlike A2's spliced donor). Paint order: [texts..., Image gauge, parked pads]. The asset-name→id map tail migrates to whichever record is last on app save; binary re-orders keep working if the image record carries it.
- **Image gauge safe writes** confirmed again: set_rect_px (rect1 only) + arr1 blend colors (set static white x6 flag=0 to kill the green/amber/red level tint on custom image art).
- **Custom image maps 1:1 onto the gauge rect** (stretch fill, no letterbox) — overlay positions = rect_x + art_px * (rect_w/art_w).
- Aerospace has NO λ glyph, RaceHead is caps-only display font — keep λ labels + unit chips (kPa/°C) on defaultdashfont.
- Sim quirk (this session): ST185 Turbo Speed stopped sweeping (stuck 0) later in the session — verified stuck on v1 too (side-by-side burst b1_77); v7's turbo gauge is byte-identical to v1's proven recipe (=V/1000, 0/255, warn180/crit190, hash 0x413F90BA). Not a v7 defect.
- App save also NORMALIZES: dropped the 2 inert pads (122->120 on v1-donor), re-serializes all records; b1_v7_guiphase.rd (`_build\backup\`) = post-GUI donor with LED asset + throttle-bound image gauge + fonts, the base for r4_v7.py.
- LED strip art: 9 LED positions per frame (not 12); frames = progressive lit states; cells at x 60-131/220-291/381-452/542-613/702-773/863-934/1024-1095/1185-1255/1345-1416, y 48-120 (of 1476x170). Sheet: `_build\assets\led_strip_sheet_1x12.png` (1x12 vertical, 1476x2040).
- GUI robustness: pyautogui.hotkey() gets swallowed; keyDown('shift')+press(n)+keyUp('shift') after clicking a gauge-list ROW works. Editor menu auto-hides fast: reveal (960,40) and menu-item click must be in the SAME script with ~0.8s gap. FILE->LOAD dialog inherits the last folder (e.g. Downloads\catch after font imports) — always type the FULL path.

## STAGE B2 — v8 + v9 (needs app)
- [x] v8 "PASNY ADU" built (b2_v8.py from b1_v7_guiphase donor): ECU-Master-ADU design language — pure black, continuous thin grid lines, rising-staircase 20-seg THROTTLE sweep (heroes Throttle + giant ENGINE LOAD numeral in RaceHead h116), ADU yellow accent strip, Aerospace grid values, Draco title. All 12 channels + TC CUT % cell (TG44 rebound to 0x5A59FBBD binarily by hash), 6 pills (FAN steady working recipe; COOL P/OIL P crit blink 1.0), CRUISE/A-C state words, no Trigger Err.
- [x] v8 NEW TECHNIQUES: (a) imported-art badge — PASNY logo cropped from corsa_CORSA_BG.png, luminance→alpha white-on-transparent, spliced over the donor's led_strip_sheet asset as frame 1 of a 1x12 sheet (b2_badge_gen.py); (b) Image Blend Color state recolor — badge image gauge rebound to TC Intervention, arr1 flag=0 [white/amber/red], sim-verified recoloring; (c) rising-staircase segmented sweep (per-segment heights 24→80px, bottom-aligned, thresholds k*5-3, top 3 segs red).
- [x] v8 iterated 4x in sim (it1 full-path load gotcha; it2 title clip + TG44 stale A/C words + pill text invisible → split housing/word records; it3 OK; it4 temps→RaceHead because AEROSPACE'S MINUS GLYPH IS A BROKEN SQUARE — verified via PIL render of all 3 fonts, RaceHead+Draco have real minus). 26-frame bursts b2v8a_*/b2v8f_*: staircase sweeps 27→98% w/ red top segs, badge white/amber/red seen, all CRUISE words (STBY/SET/RES/OVR) + A/C (OFF/REQ/ON/FLT) seen, -12°C renders w/ proper minus, pills lit+unlit, λ 2-dec Aerospace.
- [x] v8 deployed Downloads + realdash-root (258928B/121rec); backup r4_b2_st185_dash_v8_final.rd; shots r4_v8.png + r4_v8_zoom_stair_badge.png.
- [x] v9 "GLACIER AVIONICS" built (b2_v9.py from _build\b2_v9_donor.rd = asset_donor_template + BINARY FONT SPLICE, zero GUI imports — B2 headline discovery, probe-verified all 3 fonts render + needle/arc assets survive the header edit). Format: fonts live at ver_end+40 as [u32 tag=5, lpstr name, u32 size, 24B meta 00/01/46/01/1.0f/00, raw ttf] blocks BEFORE image assets [tag=0, lpstr, u32 size, 4B pad+png]; asset_count u32 @ver_end+36 (6→9); blocks copied verbatim from b1_v7_guiphase.rd (RaceHead @118..51954, Aerospace @51954..101824, Draco @101824..125066).
- [x] v9 design: cold avionics — blue-black canvas, twin HIDDEN-BEZEL floating dials (transparent roundface repaint b2_gen_glacier_face.py: no bezel, ice majors, crimson tip band 93.75-100%; A2's needle_blue.png spliced = neon-blue needle, never shipped before), heroes ENGINE LOAD (0-100 seg5) + TURBO k RPM (=V/1000 needle math, 0-160 seg5, warn140/crit150, red band = 150-160k). ENGINE LOAD HISTORY graph w/ warning shading (0/100 warn80 crit95, cyan/amber/red), TC CUT live bar (0-100 warn1 crit40) + readout, center stack λ/BOOST MAP/TC MODE, 5 cells, GLACIER Draco badge, annunciator CRUISE/A-C lenses + 6 pills (FAN steady cyan; COOL P/OIL P crit blink). RaceHead values, Aerospace labels, Draco titles. Record order copied from shipped v4 shape: [texts, arc(parked), graph, bar, needle, needle-clone, in-dial captions/readouts, Image Gauge 1 (parked 797,0), 2 pads] = 128 recs.
- [x] v9 iterated 3x (it1 pills overflowed past y480 + captions collided w/ dial 0-labels; it2 fixed sizes; it3 chips tightened). Verified: 26-frame burst b2v9f_* — load needle 9→88%, turbo needle+readout sweeping (sim turbo channel ALIVE again this session), graph trace w/ amber/red shading, TC CUT bar fill amber→red, λ 0.89/1.20 (amber high = donor v1 ranges kept), STBY/SET/RES + OFF/FLT states, pill montage: all 6 lit/unlit incl. COOL P+OIL P bright-crit frames and FAN steady cyan.
- [x] v9 deployed Downloads + realdash-root (324859B/128rec); backup r4_b2_st185_dash_v9_final.rd; shots r4_v9.png + r4_v9_zoom_dials_graph.png.
- [x] v5 reloaded active, SIM ON; probe .rd removed; Downloads = exactly 9 real dashes (st185_dash + v2..v9). STAGE B2 COMPLETE.

## STAGE C — Skill update + close-out
- [x] Fold round-4 learnings into skill (fonts, real-texture import, LED pills, TC display, new widgets). (SKILL.md: new Custom fonts section, subframes-static + dial-styling + image-recolor bullets, ±999 steady-pill CORRECTION, graph shading, design conventions, TC hash + EFR facts, sim FLAT/FAN correction, full-path load rule, refreshed Key files incl. 9 dashes + font donor + carbon/imported assets. reference.md: header asset table + binary font splice byte format, per-level font lp-strings, GUI font import path + coordinate trap, corrected steady-pill recipe, dial/tick-dot/graph/badge/LED recipes, TC Intervention hash, ≤119 render rule, save-normalization nuance.)
- [x] Close this ledger with ROUND 4 COMPLETE.

## Log
- 2026-07-07 22:05 (coordinator): ledger created; Stage P + Stage A1 launched in parallel.
- 2026-07-07 22:05 (A1): backed up probe_base/probe_gm/v1/v2/v3 to _build\backup\r4_20260707_220511_*; probe_base.rd removed from Downloads.
- 2026-07-07 23:00 (A1): GUI-probed on throwaway copy: **ST185: TC Intervention hash = 0x5A59FBBD** (harvested via bind+save on probe_tci.rd).
- 2026-07-07 23:35 (A1): DISCOVERY (affects v5/A2 + skill): flag=1 dynamic bg fill only engages when warn/crit ABOVE levels are finite. The round-3 "steady pill" recipe (alarms at ±999) makes the pill NEVER light — FAN is dead on v1/v2/v3/v5/probe_gm as shipped. Fix: keep flag=1 fill, blink=0, set warn=0.5/crit=0.7/below=0 → steady lit pill (blink fields are 0 so no strobe). Also: sim DOES sweep the FAN/FLAT bit channels 0..1 (old "sim never raises FLAT/FAN" note is wrong on 1.92 here).
- 2026-07-07 23:36 (A1): state-string mechanism verified in sim: bound gauge + static ON + 3 state strings renders per-level TEXT (NORM/WARN/CRIT probe) — basis for CRUISE/A-C state displays.
- 2026-07-08 00:15 (A1): GOTCHA: a text record placed at tail index 120 rendered NO text (v1 A/C word); moving it to 119 with 2 inert pads last fixed it. Empirical rule: keep ACTIVE records at index <= 119, last two records inert pads.
- 2026-07-08 00:25 (A1): v1/v2/v3 rebuilt + sim-verified (r4_v1t_*/r4_v2_*/r4_v3_* bursts). v1 sacrifice: TG62 2px header divider parked to keep 2 inert pads last.
- 2026-07-08 00:30 (A1): NOTE for A2/real-CAN: round-3 turbo gauges use ranges 0/255 warn180/crit190 — these compare the POST-math (=V/1000) display value per sim observation (182 amber, 134 white), consistent on real CAN.
- 2026-07-08 00:35 (A1): v6 built from probe_gm (probe_gm.rd + probe throwaways removed from Downloads); all 4 dashes deployed to both locations; v5 reloaded as active dash. STAGE A1 COMPLETE.
- 2026-07-08 00:39 (A2): took over; v4+v5 backed up as _build\backup\r4_20260708_003922_st185_dash_v4.rd / ..._v5.rd.
- 2026-07-08 01:05 (A2): PROBE WINS (v4p.rd): (1) gauge math '=V/1000' works on NEEDLE records -> de-zeroed dial labels (0/40/80/120/160 @ min0 max160 seg5 maxdig3); (2) arc autoscale maxdig=0 HIDES scale digits (safe write +52); (3) text records painted AFTER needle/arc/graph/bar records DO render (caption inside dial works; tail-index gotcha didn't bite at idx 125/128); (4) needle arr1 (image blend) tints roundface+bezel (neon ring), needle sprite stays white; (5) GUI ADD GAUGE->ARC x3 + in-app FILE->SAVE on the 128-record image dash did NOT truncate (131 records, pads+parked Image Gauge 1 intact, binary edits survived re-serialize) -> new arcs render, named Arc Gauge 1/2/3, unbound. (6) v4 FAN pill has the same +-999 dead-fill bug -> will fix.
- 2026-07-08 01:30 (A2): v4 DONE pending final verify. Pipeline: v4p.rd (GUI arc-add + app save) -> a2_v4_build.py (face splice roundface_v2.png, 4 arc blocks w/ tick dots, caption in dial, ext readout removed, FAN fix) -> a2_v4_it3.py red (needle.png neon splice, cream labels) -> a2_v4_it4.py (dots outside rings, zone-lit) -> st185_dash_v4.rd. Iterated 3x visually; red needle kept. NOTE: needle sprite color is BAKED in needle.png (arr2 recolors the scale LABELS on needle gauges, arr1 tints face+bezel); arc/needle scale-digit hiding = autoscale maxdig=0. Readouts nudged +8px clear of dots.
- 2026-07-08 01:35 (A2): v5 rebuilt (a2_gen_cf2.py + a2_v5_build.py -> st185_dash_v5.rd 544657B/83rec). GOTCHA (preempted): repurposed round-3 pill-HALO records of crit pills carry critical blink=1.0 @range_anchor+48 — would strobe any repurposed record entering warn/crit; zero all 3 blink slots when repurposing (zero_blink helper). FAN+FLAT fills fixed (warn .5/crit .7/below 0) and sim-verified sweeping dark->half->FULL BRIGHT blue steady w/ legend dim->white. Pills sit flush in the new square wells. Turbo tile: label 'Turbo' + 'k RPM' chip.
- 2026-07-08 01:45 (A2): STAGE A2 COMPLETE. Both dashes sim-verified (v4: needle sweep w/ red-zone needle, tick dots lighting cream/amber/red with value, all 4 arcs live, FAN lights; v5: pills lit+unlit clean in square wells, FLAT/FAN steady blue, all CRUISE/A-C words incl OFF), deployed to Downloads + realdash-root, v5 reloaded as active dash, SIM ON. New-for-skill (Stage C): (a) GUI ADD GAUGE + in-app save on a 128-rec image dash did NOT truncate — arcs CAN be added this way and binary edits survive the re-serialize; (b) needle/arc gauge-math enables de-zeroed dial scales; (c) needle arr2 = scale-label color, arr1 = face/bezel tint, needle sprite color lives in needle.png (splice to recolor); (d) autoscale maxdig=0 hides arc/needle scale digits (safe +52 write); (e) text records DO render painted after image-type records (in-dial captions); (f) roundface.png is 972x972 RGBA (face art incl bezel+ticks bakeable, red band etc); (g) zero blink slots when repurposing round-3 halo records; (h) square-well pill housings (r=2, inset 8) + exact-fit lens = clean lit corners.
- 2026-07-08 02:07 (B1): 3 fonts GUI-imported on font_donor.rd (RaceHead.ttf, Aerospace.ttf, Draco.otf — zero importer crashes); render-verified in run mode; byte format decoded (see STAGE B1 FONT BYTE FORMAT). Diff snapshots: _build\backup\b1_fontdiff_0/1/2.
- 2026-07-08 02:28 (B1): v7 GUI phase saved (b1_v7_guiphase.rd): led_strip_sheet_1x12.png embedded, Image Gauge 1 subframes 1x12 bound to ST185: Throttle. Subframes proven STATIC-only on 1.92 (SUBFRAME=0 clamps to frame 1; no value animation).
- 2026-07-08 03:20 (B1): st185_dash_v7.rd "APEX NIGHT" complete after 3 visual iterations; 26-frame sim burst: LED strip sweeps progressively w/ throttle (1..9 cells, blue/green/purple/red), heroes + grid in RaceHead, labels Aerospace, ST185/GT-FOUR/APEX badges in Draco, states + pills verified. Deployed both targets; v5 restored active + sim ON. STAGE B1 COMPLETE.
- 2026-07-08 04:25 (B2): v8 "PASNY ADU" complete (4 visual iterations, 2 sim bursts), deployed both targets. Header asset format DECODED GENERALLY (b2_hdr_dump.py): after '2.4.1' lpstr +36 = u32 asset_count, then typed blocks: tag=5 fonts (24B meta then raw ttf, size=file len), tag=0 images (size incl 4B zero pad before PNG). Sim turbo channel still stuck at 0 (B1's quirk persists; v8 turbo recipe byte-identical to proven v1). Starting v9.
- 2026-07-08 04:45 (B2): STAGE B2 COMPLETE. v9 "GLACIER AVIONICS" done (3 iterations, 26-frame burst), deployed both targets; v5 restored active + SIM ON; Downloads = 9 dashes. NEW FOR SKILL (Stage C): (a) BINARY FONT SPLICE works — no GUI font import needed on any donor: insert tag=5 blocks after asset_count u32 @ver_end+36 (format in STAGE B2 section), bump count; per-gauge activation still via G2.set_level_fonts; (b) header asset table general format: u32 count @ver_end+36, typed entries tag=5 font / tag=0 image; (c) transparent roundface repaint = hidden-bezel floating dial (needle+scale render fine over bare canvas); (d) needle gauge-math =V/1000 on a CLONE works (set_gauge_math first write after clone); (e) Aerospace has NO/broken minus glyph (square box) — RaceHead + Draco have real minus; use RaceHead for negative-capable values; (f) graph warn shading = replace_level_colors + finite warn/crit ranges, trace segments recolor above thresholds (proven in sim); (g) Image Blend state recolor: arr1 flag=0 [white/amber/red] + hash/ranges on a type-5 record recolors custom art per level (v8 badge on TC Intervention); (h) load_dash.py gotcha: pass the FULL PATH as filename — the UWP dialog inherits the last-used folder (e.g. Downloads\catch).
- 2026-07-07 22:25 (Stage P): ALL STAGE P ITEMS COMPLETE. Deliverable: `_build\research\round4_prep.md`. Key verdicts: custom fonts SUPPORTED (embed in .rd; add via Look'n Feel→Font & Text on a donor dash BEFORE byte-editing — in-app save required); EFR 7163 max = 150k rpm (user's 300k wrong, use 160k dial max); 3 carbon textures in `_build\assets\carbon\` (ShareTextures file = angled twill, first choice for v5); user folders yield 2 dash backgrounds + 12-frame shift-light strip in `_build\assets\imported\`; TC = Setting(0-4)+Intervention(%) on 0x3EF, engaged := Intervention>0.
- 2026-07-08 04:55 (C): STAGE C COMPLETE — all round-4 learnings folded into the skill (documentation only; no app or .rd files touched). SKILL.md gained: Custom fonts section (embed-in-.rd verdict, GUI vs binary-splice paths, set_level_fonts as THE render mechanism, glyph caveats, font donor), subframes-STATIC + LED-overlay rule, per-level image recolor + 1:1 art mapping, dial-styling bullet (needle gauge math / maxdig=0 / arr1-arr2-needle.png semantics / transparent roundface / tick dots), the ±999 steady-pill CORRECTION (flagged), graph warning shading, permanent design conventions (Turbo k RPM, state lenses, no Trigger Error, hero rule), TC Intervention hash + hash-harvest workflow, EFR 7163 = 150k fact, sim FLAT/FAN correction + stuck-channel caveat, ≤119 active-record rule + save-normalization nuance (128-rec image dash did NOT truncate), full-path load rule, PyAutoGUI hotkey workaround, refreshed Key files table (nine dashes v1–v9, font donors, carbon/imported assets, both ledgers). reference.md gained: header asset table byte format (asset_count @ver_end+36, tag=5 font blocks w/ 24B meta, tag=0 image blocks) + binary font splice offsets, per-level font lp-strings @arr_marker+4+128+12 + save-strips warning, GUI font-import click path + preview-coords trap, corrected steady-pill recipe + zero-blink rule, de-zeroed dial / tick-dot / graph-shading / badge / LED-strip recipes, POST-math threshold note, donor-dependent image-order hazard + map-tail migration, updated load/sim workflows. Both files verified against the skill authoring format (frontmatter intact, 137/169 lines).

# ROUND 4 COMPLETE — 2026-07-08
All stages done: P (research), A1 (v1/v2/v3/v6 fixes), A2 (v4/v5 fixes), B1 (fonts + v7), B2 (v8 + v9), C (skill fold-in + close-out). Nine dashes (st185_dash + v2–v9) built, verified in sim, and deployed to Downloads + realdash-root; v5 active, SIM ON. This ledger is CLOSED — round-5 agents: read the skill first, start a new ledger.
