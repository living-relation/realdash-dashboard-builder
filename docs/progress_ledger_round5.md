# ST185 RealDash — ROUND 5 Progress Ledger

> RULE: update after EVERY milestone (flip checkboxes, terse log lines). Prior rounds: `progress_ledger.md` (r3), `progress_ledger_round4.md` (r4) — both closed. Skill: `C:\Users\danie\.cursor\skills\realdash-dashboard-builder\`.

## Environment
- RealDash 1.92 maximized (~1936x1048), sim mode ON, active dash v5 (user may have switched — verify).
- Nine dashes v1-v9 in `C:\Users\danie\Downloads\` + `C:\projects\st185-link-ecu-config\rd-build\realdash-root\`.
- User's screenshot of the ellipse problem: `C:\Users\danie\.cursor\projects\c-projects-realdash-rd-build-plan\assets\c__Users_danie_AppData_Roaming_Cursor_User_workspaceStorage_empty-window_images_image-02053de4-1665-430a-a5a8-61c217fe0780.png` (v9 Engine Load dial rendering as wide ellipse).
- User verdict: newest dashes are the best yet. Keep everything; corrections only + one new layout.

## Task list (round-5 user feedback)
- [x] **FAN strobe audit — ALL nine dashes:** DONE + sim-verified. THREE causes fixed:
  1. v5/v8/v9 carried two repurposed halo records each (hi-coolant 3A7D877E / lo-oil AD193AD0) with critical blink=1.0 → genuine strobe when those bits hit 1. `r5_zero_blinks.py`: every blink slot on every record of all nine dashes is now 0 (re-audit clean).
  2. **Main FAN strobing cause: the flag=1 "dynamic color range" bg fill LERPS off→lit with the channel value.** Sim sweeps the fan bit as an analog 0→1→0 ramp (~1.6 s apparent period), so the pill faded continuously = user-visible strobing. Isolation probe proved it (flag=0 static pill solid; flag=1 control pulsing).
  3. Fix `r5_fan_flag0.py`: every FAN-bound record (pill bg + text groups) on all nine dashes converted to **flag=0 static per-level colors [off, lit, lit]** + ranges 0/1 warn .5 / crit .7 / below 0 + blink 0/0/0. Burst-verified on v9: FAN now hard on/off, identical lit frames back-to-back; FLAT (untouched gradient control) still fades. NOTE: real CAN bits are strictly 0/1 so flag=1 never looked wrong on the road — but flag=0 static is now the permanent FAN/status recipe (skill update pending).
- [x] **Arc tick marks = LINES not boxes:** DONE (`r5_fix_v4.py`). The 21 square dot records were repurposed in place: each is now a tiny text gauge rendering an ASCII glyph `- / | \` (closest to the tick's radial angle; box-drawing U+2500-2572 render as "?" — defaultdashfont lacks them). Threshold-lit via arr2 text colors [dim/cream/red] flag=0, same warn=tick-value trick. Coolant arc 6→5 ticks so all four arcs share quarter fractions; leftover record parked. Tick centers plotted on an ellipse in design units (y radius ×K) so the ring renders circular. Zoom-verified: thin angled lines, lit/red states correct.
- [x] **Round gauges perfectly ROUND:** DONE for v4 (2 needles 230x254→230x265, 4 arcs h=w×1.152) + v9 (2 needles 230x230→214x246.5, sized to clear the header rule). All resizes keep centers, so no label re-alignment was needed (verified on screenshots). No other dash has visible needle/arc records (binary scan of all nine). Magenta-disc calibration on v9 fixed rects: rendered disc 514x513 (ratio 1.0019 ✓). v4 dial ring fit ratio 0.957 (bezel art pollution in the fit; the calibrated formula governs). Formula recorded above + in skill.
- [x] **New dash v10 "MERIDIAN GT":** DONE (`tools/r5_v10.py` + `r5_v10_it2.py`, donor `_build\b2_v9_donor.rd`). Big center-dial classic w/ modern flat trim: warm graphite canvas, ONE large turbo dial (w=270, h=270×1.152 — circular from the start, magenta-disc verified 648x648 ratio 1.0000), Pillow face `_build\r5_meridian_face.png` (flat warm face, thin copper outer ring, porcelain majors, red band 150-160k, copper hub ring), copper-tinted needle (`r5_needle_copper.png` spliced). Left col: Throttle hero + copper bar, TARGET λ (default-font label), BOOST MAP, TC MODE, ETHANOL %, MERIDIAN badge (Draco). Right col: Engine Load hero + load-trace graph (copper/amber/red shading), COOLANT P kPa / CHARGE TEMP °C / FUEL TEMP °C chips. Under dial: TURBO + readout + k RPM chip, TC CUT + %. CRUISE/A-C copper-framed lenses w/ state words. Six pills ALL steady flag=0 static (round-5 rule), zero blink everywhere. No Trigger Error. Iterated twice (it1 layout shakeout → it2 needle tint + chip/badge fixes) + sim burst verified: dial sweep, hard on/off FAN (217,143,78 ↔ off), CRUISE words cycling, RaceHead minus renders on -7 temps.
- [x] Deploy: all ten dashes (v1-v9 touched by the FAN fix, v4/v9 by aspect/ticks, new v10) live in BOTH `C:\Users\danie\Downloads\` and `C:\projects\st185-link-ecu-config\rd-build\realdash-root\` (verified 08:05/08:19 timestamps). Screenshots in `_build\shots\`: `r5_v4.png`, `r5_v9.png`, `r5_v10.png`, zooms `r5_v4_zoom_dial.png` / `r5_v4_zoom_lineticks.png` / `r5_v9_zoom_dial.png` / `r5_v10_zoom_dial.png`, FAN steady proof `r5_fansteady_v9_frameA/B.png` (consecutive lit frames, identical) + bursts `r5f_*` / `r5v10_*`. Probe file r5_probe_aspect.rd deleted. **v9 restored active** (the dash found running at session start) + sim ON.
- [x] **Skill polish:** SKILL.md — new mandatory aspect-compensation bullet (K=1.152 + canvas-renders-1920x1000 fact), steady-pill recipe RE-CORRECTED to flag=0 static (round-5 supersedes round-4 flag=1), design conventions extended (never-blink pills, compensated rounds, line ticks), key-files updated (ten dashes, r5 tools, v10 donor, round-5 ledger). reference.md — steady-pill recipe rewritten with the flag=1 value-lerp root cause, new "Aspect compensation" recipe (formula + magenta-disc measure-adjust-verify + elliptical tick placement + tick-extreme measurement trap), line-tick glyph recipe, sim section now documents analog bit ramps + burst pixel-sampling.

## ROUND 5 COMPLETE
All five tasks done and verified. Final state: v9 active, sim ON, ten dashes deployed to both roots, backups in `_build\backup\r5_20260708_062333\`.

## Log
- 2026-07-08 06:20 (coordinator): ledger created; round-5 worker launched.
- 06:22 state verified: app up, sim ON, **active dash = v9** (not v5) — Glacier Avionics on screen, both dials visibly elliptical (matches user shot). Will restore v9 at wrap-up.
- 06:24 all nine dashes backed up to `_build\backup\r5_20260708_062333\`.
- 06:26 Task 1 audit+fix done (see checkbox). New tools: r5_fan_audit.py / r5_blink_dump.py / r5_zero_blinks.py.
- 08:05 FAN sim-verified steady: v9 burst (hard 72,200,240 ↔ off, no fade frames), v5 (71,167,255 solid across 3+ consecutive frames), v4 (brass 199,162,90 solid). r5_fan_flag0.py applied to all nine.
- 08:10 Tasks 2+3 done (see checkboxes). v4 fix script r5_fix_v4.py, v9 r5_fix_v9.py. Measurement helpers r5_fit_ellipse.py / r5_measure_ellipse.py (CAUTION: r5_measure_ellipse's tick-extreme method is unreliable — prefer the magenta-disc calibration r5_calib_probe.py/r5_calib2.py).
- 08:12 starting v10 build.
- 08:20 v10 "MERIDIAN GT" built (it1) → it2 (copper needle splice, chip/badge fixes); magenta-disc calib 648x648 ratio 1.0000; sim burst verified (dial sweep, hard on/off pills, CRUISE words, RaceHead minus).
- 08:27 deploy + final shots done; v9 reloaded (active) + sim on; skill polished; ledger closed.
- 06:40 **ASPECT COMPENSATION MEASURED (Task 3):** magenta-disc calibration (full-bleed circle spliced over roundface.png, square 230x230 rect) renders **552x479 px** ⇒ the 800x480 design canvas maps to EXACTLY 1920x1000 px (the header canvas units). Per-axis scales: x 2.4 px/unit, y 2.08333 px/unit. **FORMULA: h_design = K × w_design with K = (1920/800)/(1000/480) = 1.152 exactly** (keep the center fixed when resizing). K is canvas-header-invariant (scale-to-fit preserves it on other windows). Cross-check: v4 arc fill boxes measured 1.158/1.12 ✓. CAUTION for future measurement scripts: measuring "ring width/height" from white tick extremes is WRONG when majors sit at 157.5°/22.5° (gave a bogus 1.708) — calibrate with a full-bleed disc or row-scan the hairline ring. Probe file r5_probe_aspect.rd (throwaway, in Downloads — delete at wrap-up).
